# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Local env / secrets (keep out of GitHub)
.env
.env.*
!.env.example

# Logs and caches
*.log
.cache/
.tmp/
tmp/

# Backend secrets & runtime data (never commit real SMTP credentials)
api/config.local.php
api/logs/*.log
api/data/*.json
!api/logs/.gitkeep
!api/data/.gitkeep

# Node (if you add tooling later)
node_modules/
npm-debug.log*
package-lock.json

# Local preview leftovers
.parcel-cache/
dist/
.firebaserc
.firebase/
